import ProductRoute from './ProductRoute';
import UserRoute from './UserRoute';

// These exported routes are used in dynamic importing code existed in `Routes.ts`.
export {
    ProductRoute,
    UserRoute,
};
